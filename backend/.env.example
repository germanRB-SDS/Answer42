# ===========================================
# Answer42 - Variables de Entorno
# ===========================================
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.example .env
# 2. Genera un SESSION_SECRET seguro (ver abajo)
# 3. NUNCA commits el archivo .env
# ===========================================

# Entorno: development | production
NODE_ENV=development

# Puerto del servidor
PORT=3001

# Secreto de sesión (OBLIGATORIO cambiar en producción)
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CAMBIAR_ESTE_VALOR_genera_uno_con_crypto_randomBytes_32

# Base de datos SQLite
DB_PATH=./data/answer42.db

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_MAX=5

# Sesión
SESSION_MAX_AGE_MS=86400000

# Logging
LOG_LEVEL=debug
